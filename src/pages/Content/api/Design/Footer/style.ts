import styled from 'styled-components';

export const DesignFooterWrap = styled.div`
  display: flex;
  justify-content: space-between;
  border-top: 1px solid var(--color-border);
  padding: 8px 16px;
`;