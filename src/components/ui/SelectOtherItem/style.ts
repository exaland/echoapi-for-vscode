import { Flex } from 'antd';

import styled from 'styled-components';

export const SelectOtherItemWrapper = styled(Flex)`
  padding: 6px 8px;
  cursor: pointer;

  .text {
    margin-left: 4px;
    line-height: 14px;
  }
`;
